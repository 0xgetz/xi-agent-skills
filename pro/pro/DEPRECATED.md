# ⚠️ Deprecated directory

This `pro/pro/` directory was created by a path bug. All modules have been
moved to their correct location under [`pro/`](../). The files here are now
thin re-export shims kept only for backward compatibility.

**Use the canonical modules under `pro/` and run `pro/verify_system.py`.**
